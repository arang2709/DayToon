# CLAUDE.md — DayToon
하루를 그림일기로 출판하는 할 일 앱

---

## 프로젝트 개요
아침에 오늘 할 일을 고르고, 하루 동안 체크하고, 밤에 해낸 일이 그림으로 남는 앱.
완료한 일은 그림 한 컷이 되고, 못 한 일은 '원고 미도착' 빈칸으로 남는다.
하루가 끝나면 내가 보낸 시간이 한 페이지로 출판된다.

- **앱 이름**: DayToon
- **대상 사용자**: 계획 세우기가 부담스럽고, 할 일 목록을 오래 유지하기 어려운 사람
- **시간 단위**: 하루와 장면 — 한 가지 행동 = 그림 한 컷
- **핵심 감정**: 작은 뿌듯함(완료)과 아쉬움(빈칸) → 다음 날 다시 열고 싶은 마음
- **배포 환경**: GitHub Pages (단일 HTML 파일)

---

## 기술 스택
- **언어**: HTML / CSS / JavaScript (단일 파일, 프레임워크 없음)
- **AI**: OpenRouter API — 무료 티어(`:free`) 모델 사용 (자세한 내용은 [API 설정](#api-설정) 참고)
- **저장소**: `localStorage` (날짜별 자동 초기화)
- **배포**: GitHub Pages
- **외부 의존성 없음** — CDN, npm, 백엔드 서버 모두 사용하지 않음

---

## 5W1H 기획

| 기획 요소 | 세부 정의 | 구현 포인트 |
|---|---|---|
| **Who** | 계획 세우기가 부담스럽고 습관 유지가 어려운 사람 | 입력 최소화, 중요도 3단계로 단순화 |
| **What** | 할 일 체크 → 그림 한 컷으로 출판되는 하루 일기 | 완료/미완료 분기로 4컷 조합 |
| **Where** | PC 및 모바일 웹 브라우저 | 반응형 HTML/CSS, GitHub Pages 배포 |
| **When** | 아침(계획) → 낮(체크) → 밤(출판) | 자정 기준 자동 초기화, 보관함 이동 |
| **Why** | 빈칸의 아쉬움이 다음 날 다시 열게 만드는 동기 | 빈칸을 시각적으로 다정하게 남기기 |
| **How** | 체크한 일만 그림이 되고, 못 한 일은 원고 미도착으로 남음 | 키워드 분석 → 일러스트 자동 매칭 |

---

## 화면 구성 (4탭 + 하단 고정 네비게이션)

| 화면 | 역할 |
|---|---|
| 🏠 홈 | 할 일 입력(최대 20자) + 중요도 별점 1~3개 선택 + 추가하기 |
| ✅ 할 일 | 체크 목록(완료 시 빨간 체크박스) + 진행률 + 출판하기 버튼 |
| 📖 그림일기 | 4컷 만화 그리드 — 완료 → 이모지 일러스트 + AI 캡션 / 미완료 → 파란 점선 + "원고 미도착" 도장 + AI 하루 피드백 |
| 🗂 보관함 | 날짜별 일기 썸네일 3열 그리드, 클릭 시 그날의 4컷 + 피드백 다시 열람 |

하단 네비게이션은 항상 고정 노출, 4탭 어디서든 전환 가능. 할 일을 추가하면 자동으로 "할 일" 탭으로, 출판하면 자동으로 "그림일기" 탭으로 이동한다.

## 핵심 기능

| 기능 | 동작 |
|---|---|
| 할 일 입력 | 텍스트(최대 20자) + 중요도 별점 1~3개, 최대 4개(4컷 기준) |
| 체크 목록 | 완료 표시 토글, 새로고침 후에도 유지 |
| 4컷 출판 | 완료 → 키워드 매칭 이모지 + AI 캡션 / 미완료 → 원고 미도착 도장 빈칸 |
| 보관함 | 날짜별 일기 썸네일 저장(최대 60일) 및 다시 열기 |
| 자동 초기화 | 자정 감지 → 데이터 리셋, 전날 일기 보관함 이동 |

---

## AI 역할 및 규칙

### AI가 하는 일
1. 완료된 할 일 → 키워드 분석 → 이모지 일러스트 자동 매칭
   - 운동/헬스/달리기 → 🏃
   - 밥/식사/점심/저녁 → 🍱
   - 잠/수면/취침 → 😴
   - 공부/수업/책/과제 → 📚
   - 코딩/개발/프로그래밍 → 💻
   - 회의/미팅/발표 → 💬
   - 청소/정리 → 🧹
   - 친구/만남/약속 → 👫
   - 기타 → ⭐
2. 완료된 할 일 → 만화 캡션 생성 (20자 이내, 따뜻한 한국어)
3. 하루 마무리 → 달성률 기반 피드백 2문장 (미완료 항목 명확히 언급, 꾸짖지 않음)

### 절대 지켜야 할 규칙
- **완료하지 않은 일은 그리지 않는다** — 미완료는 반드시 빈칸
- **빈칸을 지우거나 다른 일로 채우지 않는다** — '원고 미도착' 그대로 유지
- **사용자를 꾸짖는 문구를 쓰지 않는다** — 빈칸은 다정하게 남긴다
- **모바일 화면에서 읽히게 만든다** — 반응형 필수
- **프롬프트를 바꿔도 이 기준은 유지한다**

### API 설정
- **서비스**: OpenRouter (https://openrouter.ai)
- **모델**: 무료 티어 모델(`:free` 접미사가 붙은 슬러그) 중 하나 — 코드에는 직접 채워 넣을 자리만 두고 특정 모델을 고정하지 않는다. ⚠️ 무료 모델 목록은 몇 주 단위로 교체/제거되므로, 사용 전 반드시 [openrouter.ai/models](https://openrouter.ai/models)에서 실제로 존재하는 `:free` 모델인지 확인할 것. 존재하지 않는 슬러그를 쓰면 400/404 에러가 난다.
- **엔드포인트**: `https://openrouter.ai/api/v1/chat/completions`
- **키 관리**: 코드 상단 `const OPENROUTER_KEY = ""`, `const OPENROUTER_MODEL = "..."` 에 저장. 기본값은 빈 문자열로 두고, 사용자(개발자 본인)가 직접 채워 넣는다 — Claude Code가 임의로 키 값을 채우지 않는다. 키/모델이 비어 있어도 앱은 기본 문구로 정상 동작해야 한다.
- **max_tokens**: 캡션 100 / 피드백 300

⚠️ **보안 주의**: GitHub Pages는 정적 공개 배포이므로 코드에 내장한 키는 페이지 소스로 누구나 볼 수 있다. 반드시 무료 모델 전용/저권한 키를 사용하고, 남용이 감지되면 즉시 OpenRouter 대시보드에서 키를 폐기·재발급할 것.

---

## 디자인 규칙
초등학교 그림일기장 감성 — 누런 종이 + 크레파스 질감 + 손그림 테두리

- **배경**: 크림/베이지 (#FFF8E7)
- **테두리**: 삐뚤빼뚤한 손그림 느낌, 굵은 테두리 + 오프셋 그림자
- **폰트**: Google Fonts — Noto Sans KR
- **색상 포인트**: 빨강 `#E85555` / 노랑 `#F5C842` / 초록 `#5BAD6F` / 하늘 `#5B9AD5`
- **레이아웃**: 모바일 우선, `max-width: 390px` 중앙 정렬 + 하단 고정 4탭 네비게이션
- **4컷 레이아웃**: 2열 그리드 / 완료 칸 = 이모지 일러스트 + 캡션 / 미완료 = 파란 점선 + "원고 미도착" 도장 스타일
- **보관함**: 3열 썸네일 그리드
- **캐릭터**: 귀여운 강아지 🐶 곳곳 배치, 말풍선으로 응원 메시지

---

## 파일 구조

```
daytoon/
├── index.html       ← 전체 앱 (HTML + CSS + JS 한 파일)
└── CLAUDE.md         ← 이 기획 문서 (Claude Code 자동 로드)
```

---

## 코딩 규칙
- **단일 파일 원칙** — `index.html` 하나에 모든 코드 작성, 외부 파일 분리 금지
- **프레임워크 금지** — React, Vue 등 사용 안 함. 순수 JS만
- **상태 관리** — `tasks` 배열 하나로 모든 상태 관리, 변경 시 항상 localStorage 저장
- **날짜 초기화** — 앱 시작 시 localStorage의 날짜와 오늘을 비교, 다르면 전날 일기를 보관함으로 옮기고 전체 리셋
- **API 키 관리** — OpenRouter 무료 모델 키를 `index.html` 상단 상수로 하드코딩, 사용자 입력 UI는 두지 않음 (자세한 내용은 [API 설정](#api-설정) 참고)
- **한국어 UI** — 모든 텍스트, 버튼, 피드백은 한국어로

---

## 배포

1. GitHub 레포 생성 (Public, 이름: `daytoon`)
2. `index.html` + `CLAUDE.md` 파일 push
3. Settings → Pages → Branch: `main` / `(root)` → Save
4. 배포 주소: `https://[유저명].github.io/daytoon`

API 키는 사용자 입력 없이 코드에 내장된 OpenRouter 무료 모델 키를 그대로 사용한다. 공개 저장소에 올라가는 만큼 무료/저권한 키만 사용할 것 (자세한 내용은 [API 설정](#api-설정) 참고).
